{
  "name": "autogate-nx",
  "version": "1.0.0",
  "private": true,
  "description": "AutoGate NX — IT park vehicle access control demo (Next.js UI + Electron desktop build).",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "app": "npm --prefix frontend run build && npm --prefix desktop start",
    "exe": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix desktop install && npm --prefix desktop run dist",
    "exe:fast": "npm --prefix frontend run build && npm --prefix desktop run dist:dir"
  }
}
