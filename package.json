{
  "name": "autogate-nx",
  "version": "1.0.0",
  "private": true,
  "description": "AutoGate NX — IT park vehicle access control demo (Next.js UI + Electron desktop build).",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "app": "cd frontend && npm install && npm run build && cd .. && cd desktop && npm install && npm start",
    "exe": "cd frontend && npm install && npm run build && cd .. && cd desktop && npm install && npm run dist",
    "exe:fast": "cd frontend && npm run build && cd .. && cd desktop && npm run dist:dir"
  }
}
